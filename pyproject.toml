[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "scraut"
version = "0.1.0"
requires-python = ">=3.11"

[project.scripts]
scraut = "scraut.cli.cli:main"

[tool.setuptools.packages.find]
where = ["apps/automation"]
include = ["scraut*"]
